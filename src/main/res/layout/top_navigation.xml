<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <ImageView
        android:id="@+id/calendarNavButton"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginRight="20dp"
        android:layout_marginEnd="20dp"
        app:tint="@color/colorAccent"
        app:layout_constraintBottom_toBottomOf="@+id/graphsNavButton"
        app:layout_constraintEnd_toStartOf="@+id/graphsNavButton"
        app:layout_constraintTop_toTopOf="@+id/graphsNavButton"
        app:srcCompat="@drawable/ic_baseline_calendar_today_24" />

    <ImageView
        android:id="@+id/graphsNavButton"
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/design_default_color_primary"
        app:srcCompat="@drawable/ic_baseline_timeline_24" />
</androidx.constraintlayout.widget.ConstraintLayout>