<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aboutme">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/rough_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rough_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityHelp" />
        <activity android:name=".Sleep" />
        <activity android:name=".Symptoms" />
        <activity android:name=".Diet" />
        <activity android:name=".Medication" />
        <activity android:name=".Calendar" />
        <activity android:name=".CalendarDate" />
        <activity android:name=".Graphs" />
    </application>

</manifest>